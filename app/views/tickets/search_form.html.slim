- content_for :admin_list do
	h1 Full Text Search
	div
		= form_tag ticket_search_path
			div

				= text_field_tag :query, params[:query], placeholder: "Aperiam nam ipsa."
			div
				= button_tag
	div
		= render(@tickets) || "Nothing found"

= render :template => 'layouts/admin_layout'